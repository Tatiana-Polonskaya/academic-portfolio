/* eslint-disable no-unused-vars */
export enum Routers {
    Main = "/",
    Achievements = "/achievements",
    Achievement = "/achievements/:id",
    Articles = "/articles",
    Article = "/articles/:id",
}
